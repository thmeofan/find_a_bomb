import 'package:flutter/material.dart';

class SetScreenScreen extends StatelessWidget {
  const SetScreenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
